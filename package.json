{
  "name": "folia-kb",
  "version": "0.1.6",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build && mkdir -p .next/standalone/.next && cp -R .next/static .next/standalone/.next/static",
    "start": "next start",
    "lint": "eslint",
    "release:package": "node scripts/release-package.js",
    "release:minor": "node scripts/update-changelog.js minor && npm version minor -m \"chore(release): %s\" && git push && git push --tags",
    "release:major": "node scripts/update-changelog.js major && npm version major -m \"chore(release): %s\" && git push && git push --tags",
    "release:patch": "node scripts/update-changelog.js patch && npm version patch -m \"chore(release): %s\" && git push && git push --tags"
  },
  "dependencies": {
    "@fortawesome/fontawesome-svg-core": "^7.1.0",
    "@fortawesome/free-solid-svg-icons": "^7.1.0",
    "@fortawesome/react-fontawesome": "^3.1.1",
    "@uiw/react-md-editor": "^4.0.11",
    "next": "16.1.4",
    "react": "19.2.3",
    "react-dom": "19.2.3"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.1.4",
    "tailwindcss": "^4",
    "typescript": "^5"
  }
}
